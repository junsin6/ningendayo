# 推敲サマリ — 2026-07-05-001（ベクトルデータベース技術解説記事・敬体）

最終稿: `final.md`。判定経路: detect → rewrite → fidelity(pass) ＋ naturalness(B) → **accept（change_rate override）**。

## スコア変化
- severity_weighted_score: 73.3 → 4.2（改善率 94.3%）
- 検出 43 件（S1×18 / S2×25）→ 残存 S1 0 / S2 4（E-2 文末単調・シャーディング・レプリケーション・「必要があります」）/ S3 0
- change_rate（difflib）47.6%相当（0.48）／ lexical 0.42 ／ deletion 0.35 ／ similarity 0.73（正味差 27%）

## change_rate override の理由（IMP-001 既知欠陥）
- difflib change_rate 0.48 は 30% 警告を超えるが 50% 中断未満。数字が膨らんだ実体は **カタカナ→漢語の1対1置換（挿入+削除で二重計上）** と冗長文法（ことができる×4・万能動詞連鎖・状態叙述×3）の圧縮であり、fidelity-auditor が **情報欠落ゼロ・13/13 pass** を確認。similarity 0.73 が体感に近い。fidelity=pass かつ自然度 B のため **override accept**。

## 主要変更
- B-2 カタカナ語 19 件 → 17 件を日本語化（埋め込み／幻覚／注入／文脈／知識ベース／拡張性／構成要素／作業負荷／調整 等）。RAG・HNSW・AI・モデル・アルゴリズム・アーキテクチャは固有/標準語として維持。
- A-10 抽象主語+万能動詞 4 件（可能にする／提供する／もたらす／役割を果たす）→ 具体動詞（使えば検索できます／得られます／高めます／中核を担う）。
- A-5「することができる」×4 → 全て「できる」。A-6 状態叙述×3・D-1 二重ヘッジ結び → 断定へ。
- C-1「まず・次に・最後に」→「まず」1つのみ残し他を順接連鎖へ溶解。RAG の処理順序（取得→注入→生成）は完全保存。

## fidelity 判定
- f002「欠かせない存在」→「重要な存在」（indispensable→important の一段軟化）は **許容**。事実命題でなく主観的評価で極性不変、D-4 ハイプ常套句の正当な平叙化。f042「非常に重要である→重要です」も同論理で許容。ロールバックなし。

## 残課題
- E-2 文末単調が S2 で残存（敬体の構造的天井）。体言止め 1〜2 箇所で A 到達可能だが、純敬体で体言止めを差すと常体感が混じるため rewriter は語尾語彙分散で対処。過推敲回避を優先し B で accept。
- シャーディング/レプリケーションは業界標準語として残置（B-2 免責候補）。この 2 件の去就だけで A↔B が反転する境界脆弱性あり（IMP へ）。
- 等級 B / accept。2 次推敲は必須でない。
